import React from 'react';
import Button from '@mui/material/Button';

const Contato = ({button}) => {
    return ( <Button variant="text" className='contat' >{button}</Button> );
}
 
export default Contato;